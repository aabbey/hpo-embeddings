def pass_list(in_list: list) -> list:
    """Pass a list of strings to another function

    Args:
        in_list: list of strings
    """
    return in_list


def get_term(single_hpo_term: str) -> str:
    """Get a single HPO term as a string

    Args:
        single_hpo_term (str): single HPO term that matches patient phenotype

    Returns:
        str: returns the same string
    """
    return single_hpo_term
